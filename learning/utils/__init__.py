"""Self-contained training utilities (de-lerobot'd).

Mirrors the ``lerobot.utils`` / ``lerobot.rl`` surface ALLEX uses: ``seed.set_seed``,
``logging_utils.init_logging`` / ``format_big_number``, ``train_utils.save_checkpoint`` /
``update_last_checkpoint``, ``wandb_logger.WandBLogger``, ``import_utils.register_third_party_plugins``.
"""
