"""Self-contained training config + CLI parser (de-lerobot'd).

Mirrors the ``lerobot.configs`` surface ALLEX uses: ``config.TrainPipelineConfig`` and
``parser.wrap()`` / ``parser.parse``. See the module docstrings for the reproduced fields.
"""
