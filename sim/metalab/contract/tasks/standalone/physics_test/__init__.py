"""Standalone physics probes — scenes built to measure solver/contact behaviour, not to manipulate.

No shared ``_base`` here: there is one contract, so there is nothing two of them state identically yet."""
