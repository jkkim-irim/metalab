"""Standalone-only scene contracts (no learning) — the Launchpad's Standalone mode.

Kept OUT of the train/eval task list (the launchpad and rl_train.sh glob only the top-level tasks/
folder); the loader resolves these as a fallback when a top-level task module is absent."""
