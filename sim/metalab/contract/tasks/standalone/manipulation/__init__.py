"""Standalone manipulation scenes — a robot at a desk with something on it, one contract per object.

Shared core in ``_base.py`` (robot, init pose, desk, physics, contact params, camera, action groups); a
contract states only what differs from it."""
